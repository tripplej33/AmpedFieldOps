# AmpedFieldOps Frontend Environment Variables
# Copy this file to .env.local and fill in your values
# Vite requires the VITE_ prefix for environment variables

# ============================================
# REQUIRED - Supabase Configuration
# ============================================
# Supabase URL
# For local development: http://127.0.0.1:54321
# For production: https://your-domain.com or http://your-server-ip:8000
VITE_SUPABASE_URL=http://127.0.0.1:54321

# Supabase CLI / server-side variables
# Supabase HTTP API URL (local Supabase uses 127.0.0.1:54321)
SUPABASE_URL=http://127.0.0.1:54321

# Database connection URL (Postgres). Example local default:
# postgresql://postgres:postgres@127.0.0.1:54322/postgres
DATABASE_URL=

# Supabase service role key (keep secret, used by backend)
SUPABASE_SERVICE_ROLE_KEY=

# Supabase Anonymous Key (public, safe for frontend)
# For local Supabase: Use the default anon key (already in code as fallback)
# For production: Get from Supabase dashboard
# 
# Local default (already in code):
# eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
VITE_SUPABASE_ANON_KEY=

# ============================================
# OPTIONAL - Build Configuration
# ============================================
# Base path for the application (for subdirectory deployments)
# Default: / (root)
VITE_BASE_PATH=/

# --- Security notes ---
# The project no longer requires a custom `JWT_SECRET` for Supabase-managed auth.
# The backend contains a local fallback for development only. For any production
# deployment, explicitly set strong secrets and do NOT commit them to git.
# Example (backend .env):
# JWT_SECRET=your-production-jwt-secret-min-32-chars
